---
phase: 01-foundation-stabilization
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/features/runtime/types/runtime.ts
  - src/features/runtime/server/get-runtime-overview.ts
  - src/features/runtime/components/RuntimeStatusPanel.tsx
  - src/features/workbench/components/OverviewPageContent.tsx
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
user_setup: []
must_haves:
  truths:
    - RuntimeOverview 暴露统一 summary，避免组件侧重复推导
    - overview 页面能看到面向使用者的 runtime readiness 明细
  artifacts:
    - RuntimeOverview summary 字段
    - Overview 页面上的 runtime panel
  key_links:
    - get-runtime-overview 生成 summary
    - RuntimeStatusPanel 消费 summary 和 dependency detail
---

<objective>
让运行时状态既有机器可依赖的 summary，又有概览页可直接阅读的 readiness 面板。

Purpose: 让 Phase 1 的“当前模式和服务 readiness 对用户可见”真正落地，而不是只有原始枚举状态。
Output: 扩展 RuntimeOverview、改造 runtime 面板、接入 overview 页面。
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/features/runtime/types/runtime.ts
@src/features/runtime/server/get-runtime-overview.ts
@src/features/runtime/components/RuntimeStatusPanel.tsx
@src/features/workbench/components/OverviewPageContent.tsx
</context>

<tasks>

<task type="auto">
  <name>为 RuntimeOverview 增加 readiness summary</name>
  <files>src/features/runtime/types/runtime.ts, src/features/runtime/server/get-runtime-overview.ts</files>
  <read_first>src/lib/env.ts, src/features/runtime/types/runtime.ts</read_first>
  <action>新增 runtime summary 结构，集中输出 configured/missing/mock 计数、是否 ready、用户可读 label/detail。</action>
  <verify>getRuntimeOverview 返回包含 summary 字段。</verify>
  <acceptance_criteria>
    - RuntimeOverview 类型包含 summary
    - summary 提供 label/detail 与计数字段
  </acceptance_criteria>
  <done>组件无需再各自推导 readiness 统计。</done>
</task>

<task type="auto">
  <name>升级 runtime 面板并接入总览页</name>
  <files>src/features/runtime/components/RuntimeStatusPanel.tsx, src/features/workbench/components/OverviewPageContent.tsx</files>
  <read_first>src/features/runtime/server/get-runtime-overview.ts, src/features/knowledge/components/KnowledgeOverviewPanel.tsx</read_first>
  <action>在 runtime 面板中展示 summary、依赖状态说明和 detail 文案，并把面板接入 overview 页面，与知识库概览形成“内容 + 运行时”双视角。</action>
  <verify>OverviewPageContent 左侧栏能同时看到 KnowledgeOverviewPanel 和 RuntimeStatusPanel。</verify>
  <acceptance_criteria>
    - RuntimeStatusPanel 渲染 summary label/detail
    - dependency card 渲染 detail
    - overview 页面接入 runtime 面板
  </acceptance_criteria>
  <done>用户打开总览页就能同时看见知识库状态与运行时 readiness。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] Overview 页面接入 runtime 面板
</verification>

<success_criteria>

- RuntimeOverview summary 建模完成
- Overview 页面展示 runtime readiness 明细

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-stabilization/01-03-SUMMARY.md`
</output>
