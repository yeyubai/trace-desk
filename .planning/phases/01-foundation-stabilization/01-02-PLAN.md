---
phase: 01-foundation-stabilization
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/features/workbench/components/DashboardTopBar.tsx
  - src/features/workbench/components/WorkspaceStatusStrip.tsx
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
user_setup: []
must_haves:
  truths:
    - 所有 dashboard 子页都共享同一套顶部导航与工作台状态条
    - 用户无需看代码就能知道当前 data mode、AI mode 与依赖 readiness
  artifacts:
    - src/features/workbench/components/WorkspaceStatusStrip.tsx
  key_links:
    - dashboard layout 在 top bar 下方挂载统一状态条
---

<objective>
把运行时模式和工作台关键信号抬到统一壳子层，确保总览、导入、问答、会话四个页面共享一致体验。

Purpose: 让工作台在所有子页都可感知当前模式与 readiness，而不是只在局部组件里零散显示。
Output: 统一状态条组件与 layout 接入。
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(dashboard)/layout.tsx
@src/features/workbench/components/DashboardTopBar.tsx
@src/features/runtime/server/get-runtime-overview.ts
@src/features/workbench/server/getWorkbenchSnapshot.ts
</context>

<tasks>

<task type="auto">
  <name>设计跨页面工作台状态条</name>
  <files>src/features/workbench/components/WorkspaceStatusStrip.tsx</files>
  <read_first>src/features/runtime/types/runtime.ts, src/features/workbench/types/workbench.ts</read_first>
  <action>新增一个紧凑状态条，展示数据模式、AI 模式、依赖 readiness 摘要、来源数、会话数与最近索引时间，文案面向使用者而不是开发者。</action>
  <verify>状态条组件能接收 runtime、sourceCount、sessionCount、lastIndexedAt 并渲染关键信息。</verify>
  <acceptance_criteria>
    - 状态条至少显示 data mode、AI mode、readiness summary
    - 状态条至少显示来源/会话或最近索引信号
  </acceptance_criteria>
  <done>状态条足以回答“当前工作台跑在什么模式、缺什么、现在有什么内容”。</done>
</task>

<task type="auto">
  <name>把状态条接入 dashboard layout</name>
  <files>src/app/(dashboard)/layout.tsx, src/features/workbench/components/DashboardTopBar.tsx</files>
  <read_first>src/features/workbench/components/WorkspaceStatusStrip.tsx</read_first>
  <action>保持现有导航结构，同时把统一状态条挂到 layout，使 overview/import/chat/sessions 页面天然共享这层运行时信息。</action>
  <verify>DashboardLayout 服务端获取 snapshot 后，把知识库名和状态条所需字段都传给壳子组件。</verify>
  <acceptance_criteria>
    - layout 中 top bar 下方存在状态条
    - DashboardTopBar 仍保持当前导航与主操作入口
  </acceptance_criteria>
  <done>四个 dashboard 子页不需要各自重复渲染模式摘要。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] dashboard layout 渲染包含统一状态条
</verification>

<success_criteria>

- dashboard shell 在所有子页保持一致
- 顶部区域稳定展示运行时模式和 readiness 摘要

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-stabilization/01-02-SUMMARY.md`
</output>
