---
phase: 01-foundation-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-foundation-stabilization/01-CONTEXT.md
  - src/features/workbench/schemas/workbench-snapshot.ts
  - src/features/workbench/server/getWorkbenchSnapshot.ts
  - src/features/workbench/hooks/useWorkbenchSnapshotQuery.ts
  - src/features/knowledge/hooks/useKnowledgeMutations.ts
  - src/features/chat/hooks/useChatMutations.ts
  - src/lib/api.ts
autonomous: true
requirements:
  - FOUND-03
user_setup: []
must_haves:
  truths:
    - WorkbenchSnapshot 在服务端输出和客户端消费两侧都有显式校验
    - import/upload/chat complete 返回的 snapshot 都复用同一份解析逻辑
  artifacts:
    - src/features/workbench/schemas/workbench-snapshot.ts
  key_links:
    - useWorkbenchSnapshotQuery / useKnowledgeMutations / useChatMutations 都连接到同一份 snapshot parser
---

<objective>
为工作台基础数据建立稳定契约，并把当前 Phase 1 的上下文和边界落为可复用计划。

Purpose: 先锁住 `WorkbenchSnapshot` 契约，后续 Phase 2-5 才能在不反复猜接口结构的前提下推进。
Output: Phase 1 上下文文档、snapshot schema、客户端 parser 接入。
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/features/workbench/server/getWorkbenchSnapshot.ts
@src/features/workbench/hooks/useWorkbenchSnapshotQuery.ts
@src/features/knowledge/hooks/useKnowledgeMutations.ts
@src/features/chat/hooks/useChatMutations.ts
@src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>写入 Phase 1 执行上下文</name>
  <files>.planning/phases/01-foundation-stabilization/01-CONTEXT.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, AGENTS.md</read_first>
  <action>把 Phase 1 的边界、决策、可复用代码入口和延期项写清楚，确保后续计划和实现都能复用同一份语义。</action>
  <verify>上下文文件存在并包含 Phase Boundary、Implementation Decisions、Canonical References。</verify>
  <acceptance_criteria>
    - 01-CONTEXT.md 包含工作台壳子、运行时状态、数据契约 3 个决策区
  </acceptance_criteria>
  <done>下游 agent 或人工执行者只看 CONTEXT 也能理解本 phase 的边界。</done>
</task>

<task type="auto">
  <name>为 WorkbenchSnapshot 建立 zod 契约</name>
  <files>src/features/workbench/schemas/workbench-snapshot.ts, src/features/workbench/server/getWorkbenchSnapshot.ts</files>
  <read_first>src/features/workbench/types/workbench.ts, src/features/chat/types/chat.ts, src/features/knowledge/types/knowledge.ts</read_first>
  <action>创建覆盖 workbench、chat、knowledge、runtime 的 snapshot schema，并在服务端聚合函数返回前执行 parse。</action>
  <verify>TypeScript 编译通过，schema 文件被服务端聚合函数引用。</verify>
  <acceptance_criteria>
    - getWorkbenchSnapshot 返回值通过 parseWorkbenchSnapshot
    - snapshot schema 覆盖 sessions、sources、runtime、feedbackSummary
  </acceptance_criteria>
  <done>后续 mock/live 只要不满足 schema，就会在基础层被发现。</done>
</task>

<task type="auto">
  <name>把客户端 snapshot 消费统一接到 parser</name>
  <files>src/features/workbench/hooks/useWorkbenchSnapshotQuery.ts, src/features/knowledge/hooks/useKnowledgeMutations.ts, src/features/chat/hooks/useChatMutations.ts, src/lib/api.ts</files>
  <read_first>src/features/workbench/schemas/workbench-snapshot.ts, src/lib/api.ts</read_first>
  <action>给 fetch helper 增加可选 parser，并让 query、导入 mutation、chat complete snapshot 都复用同一份解析逻辑。</action>
  <verify>查询与 mutation 代码不再直接信任未解析的 snapshot JSON。</verify>
  <acceptance_criteria>
    - useWorkbenchSnapshotQuery 使用 parser
    - import/upload mutations 使用 parser
    - chat complete 事件进入缓存前会解析 snapshot
  </acceptance_criteria>
  <done>客户端不再有“裸 as 类型断言”的 snapshot 消费路径。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] 相关 hook / server 文件都已接入 snapshot parser
</verification>

<success_criteria>

- Phase 1 上下文文件创建完成
- WorkbenchSnapshot 契约建立并在服务端/客户端两侧生效
- import/upload/chat complete 三条 snapshot 回流路径一致

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-stabilization/01-01-SUMMARY.md`
</output>
